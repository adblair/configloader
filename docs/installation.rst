============
Installation
============

At the command line::

    $ easy_install configloader

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv configloader
    $ pip install configloader
